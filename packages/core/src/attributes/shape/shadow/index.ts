/** Shadow prop and resolved types, resolver, and array lerp. */
export type { ShadowProp, ShadowResolved } from './resolver';
export { resolveShadow, resolveShadowArray } from './resolver';

export { lerpShadowArray } from './lerp';
