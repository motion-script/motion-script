/** Stroke prop and resolved types, resolver, and array lerp. */
export type { StrokeProp, StrokeResolved, StrokeAlign } from './mapper';
export { resolveStroke, resolveStrokeArray, resolveStrokeAlign } from './mapper';

export { lerpStrokeArray } from './lerp';
