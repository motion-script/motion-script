/** Stroke prop and resolved types, resolver, and array lerp. */
export type { Stroke, StrokeProp, StrokeResolved, StrokeAlign, StrokeCap, StrokeJoin } from './mapper';
export { resolveStroke, resolveStrokeArray, resolveStrokeAlign, resolveStrokeCap, resolveStrokeJoin } from './mapper';

export { lerpStrokeArray } from './lerp';
