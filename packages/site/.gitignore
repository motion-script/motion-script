# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Standalone player app bundle embedded by /editor — generated by
# `pnpm --filter @motion-script/player build:app`. A build artifact, not source.
/static/player

# API reference pages generated from each package's TypeScript source by
# `docusaurus-plugin-typedoc` (see docusaurus.config.ts) and served by the
# dedicated `api` docs instance. Regenerated on every `start`/`build` — a build
# artifact, not source.
/api

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
