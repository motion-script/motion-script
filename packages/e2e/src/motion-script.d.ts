// The `*?scene` module declaration lived here for the vite-plugin's scene
// transform. Scenes are plain modules now, so there is nothing to declare —
// this file stays only as the place a project-wide ambient type would go.
export {};
